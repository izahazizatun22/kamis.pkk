<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login - Spice Dums</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="icon" href="/images/logo.png" type="image/png" />
</head>
<body class="auth-page">
  <div class="auth-card">
    <div class="auth-back"><a href="/#menu">← Kembali ke Menu</a></div>
    <img src="/images/logo.png" alt="logo" class="logo-img" />
    <h2>LOGIN</h2>
  <p>Masuk untuk melihat menu, review, dan melakukan pemesanan.</p>
  <% if (typeof error !== 'undefined' && error) { %>
    <div style="margin: 8px 0; color: #c62828; font-weight: 600;"><%= error %></div>
  <% } %>
  <form method="post" action="/login">
      <label>Username</label>
      <input name="username" value="<%= typeof username !== 'undefined' ? username : '' %>" placeholder="Masukkan username" autocomplete="username" required />
      <label>Password</label>
      <input type="password" name="password" placeholder="Masukkan password" autocomplete="current-password" required />
      <button type="submit" class="btn">Login</button>
    </form>
    <div class="auth-switch">Belum punya akun? <a href="/register">Daftar</a></div>
  </div>
</body>
</html>
